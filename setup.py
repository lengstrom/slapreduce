#!/usr/bin/env python
from setuptools import setup

setup(name="slapreduce",
      version="0.1",
      description="slapreduce",
      packages=['slapreduce'],
      include_package_data=True,
      )
